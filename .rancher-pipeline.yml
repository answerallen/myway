stages:
- name: build
  steps:
  - publishImageConfig:
      dockerfilePath: ./Dockerfile
      buildContext: .
      tag: myway
      pushRemote: true
      registry: index.docker.io
timeout: 60
notification: {}
